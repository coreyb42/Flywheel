org.gradle.jvmargs = -Xmx3G
org.gradle.daemon = false
# Architectury Loom's non-remapping mode is required for Minecraft 26.2, whose
# version metadata has no obfuscation mappings. Keep this explicit because the
# legacy multi-project plugin setup resolves its marker too late.
fabric.loom.disableObfuscation = true
# Common metadata
mod_license = MIT
mod_sources = https://github.com/Engine-Room/Flywheel
mod_issues = https://github.com/Engine-Room/Flywheel/issues
mod_homepage = https://github.com/Engine-Room/Flywheel
# Flywheel metadata
flywheel_id=flywheel
flywheel_name=Flywheel
flywheel_version=1.0.6
flywheel_description=An overhauled entity and block entity rendering API.
# Vanillin metadata
vanillin_id=vanillin
vanillin_name=Vanillin
vanillin_version=1.1.3
vanillin_description=Instanced rendering for entities and block entities via Flywheel.
# Vanillin dependencies
flywheel_maven_version_range=[1.0.0,2.0)
flywheel_semver_version_range=>=1.0.0 <2.0.0

# Mod dependency declarations
minecraft_semver_version_range = >=26.2 <26.3
minecraft_maven_version_range = [26.2,26.3)
fabric_api_version_range = >=0.160.0
neoforge_version_range = [26.2.0.88,)

# General build dependency versions
java_version = 25
arch_loom_version = 1.17.493
cursegradle_version = 1.4.0
# Minecraft 26.2 ships official Mojang mappings; Parchment has no 26.2 layer.
parchment_version = none

# Minecraft build dependency versions
minecraft_version = 26.2
neoforge_version = 26.2.0.88
fabric_loader_version = 0.19.5
fabric_api_version = 0.160.0+26.2

# Build dependency mod versions
sodium_version = mc26.2-0.9.2
iris_version = 1.11.4+26.2
# There is no oculus for 1.21.1 so we will only support iris
embeddium_version = 1.0.11+mc1.21.1

# Publication info
flywheel_group=dev.engine-room.flywheel
vanillin_group=dev.engine-room.vanillin
artifact_minecraft_version = 26.2
